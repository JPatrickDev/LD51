package me.jack.ld51.Entity.Particles.Decorative;

import com.badlogic.gdx.graphics.Color;

import me.jack.ld51.Entity.Particles.Particle;

public class DecorativeParticle extends Particle {
    public DecorativeParticle(int x, int y, int w, int h, Color c1, Color c2) {
        super(x, y, w, h, c1, c2);
    }
}
